/**
 * GESSOF Portal — Main Application Entry Point
 *
 * NOTA: La lógica principal del portal está embebida en index.html (script inline).
 * Este archivo existe como punto de entrada futuro para cuando se modularice el código.
 *
 * Módulos planificados (pendientes de creación):
 *   ./core/auth.js     → Autenticación y permisos
 *   ./core/storage.js  → Persistencia de datos (localStorage / Supabase)
 *   ./core/router.js   → Navegación entre secciones
 *   ./core/theme.js    → Gestión de tema claro/oscuro
 *
 * ESTADO: Este archivo NO se carga actualmente en index.html.
 *         Es seguro ignorarlo hasta la migración a módulos ES6.
 */

console.info('[GESSOF] app.js cargado — módulos pendientes de implementación.');
